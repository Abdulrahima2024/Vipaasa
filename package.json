{
  "name": "vipaasa-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "turbo": "^1.12.4"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "cloudinary": "^2.10.0",
    "ioredis": "^5.11.1"
  },
  "optionalDependencies": {
    "@next/swc-win32-x64-msvc": "^16.2.9"
  }
}
